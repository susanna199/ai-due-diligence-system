# ─── LegalDoc AI — Environment Variables ───────────────────────────────────
# Copy this to .env and fill in your key.

# EMBEDDINGS: always local (HuggingFace all-MiniLM-L6-v2) — no key needed.

# LLM Provider for compliance checks & advisory report generation.
# Recommended: anthropic  (Claude Haiku — affordable, fast)
# Alternative: openai     (gpt-4o-mini — only if you have paid quota)
LLM_PROVIDER=anthropic

# Anthropic API key  →  https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API key (optional — only if LLM_PROVIDER=openai)
# WARNING: free-tier quota is very low and breaks during PDF indexing
# OPENAI_API_KEY=your_openai_api_key_here
